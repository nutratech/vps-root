# git.nutra.tk - DNS now points directly to prod (216.218.216.163)
# This file kept as a placeholder; remove once migration is fully verified.
#
# To restore if needed, check git history:
#   git log --oneline -- etc/nginx/conf.d/dev/git-http.conf

# Rate limiting zone (referenced by other configs, keep definition)
# NOTE: if gitweb_limit is defined elsewhere (e.g. ratelimit.conf), remove this.
